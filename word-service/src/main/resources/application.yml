server:
  port: 8083
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: word-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:db_words}
    username: ${DB_USER:motus}
    password: ${DB_PASS:motus}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info
